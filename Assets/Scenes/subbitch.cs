using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class subbitch : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Sub, bitch");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
